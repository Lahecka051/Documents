### Slide 1. X-VLA

안녕하십니까, 강재모 교수님 연구실 석박사과정 이강민입니다. 오늘 발표할 논문은 ICLR 2026에 게제된 논문인 X-VLA에 대해서 설명하겠습니다.

X-VLA는 서로 다른 로봇의 데이터를 하나의 모델로 학습하고, 새로운 로봇에 적응시키는 방법을 다룬 논문입니다. 먼저 VLA의 행동을 생성하는 원리부터 설명하겠습니다.

### Slide 2. VLA Architecture

VLA는 영상과 언어 지시를 입력 받아 로봇의 행동을 예측하는 모델입니다.

그림에서 왼쪽부분은 장면과 지시를 이해하고, 오른쪽에서 앞으로 실행할 행동 묶음을 만들게 됩니다. 이후에 로봇 컨트롤러가 이를 실행하면 새로운 관측이 들어오게되고, 이 과정을 반복하는 것이 일반적인 로봇의 폐루프 동작입니다.

### Slide 3. Type of Method

로봇의 행동을 만드는 방법은 크게 두가지로 나뉩니다.

OpenVLA 모델은 행동 토큰을 순차적으로 생성하고. 아래의 π₀는 별도의 Action Expert가 Action Chunk를 반복적으로 갱신하면서 정답에 가까운 행동을 예측합니다.

### Slide 4. Action Vector & Action Chunk

X-VLA는 한 팔의 손끝 위치 x,y,z, 모터 회전 값 6개, Gripper 1개로 행동을 표현합니다. 

흐름도를 보시면 VLA Policy에서 나온 Action Vector를 여러 행으로 쌓으면 Action Chunk가 됩니다.

### Slide 5. Difference in Approach

다시 모델로 돌아가서 모델을 조금 더 세분화하면 세가지로 나눌 수 있습니다.

첫번째 방식과 세번째 방식은 앞서 설명을 했고 두번째 방식은 첫번째 방법의 순차적 생성을 Action Chunk로 묶어서 전체를 한번에 병렬로 예측합니다.

### Slide 6. Flow Matching

Flow Macthing의 학습과 추론 과정에 대한 설명입니다. 학습은 노이즈와 정답 시연을 섞어 중간 상태를 만들고, 모델이 어느 방향으로 값을 바꿔야 즉 노이즈를 제거하고 정답 얼마나 잘 예측하는지를 학습합니다.

추론에서는 정답을 주지 않고, 노이즈에서 시작해 모델의 예측으로 반복 갱신합니다.

''' (물어보면 말하기 아니면 그냥 넘어가기)
여기서 small "t"는 생성 진행도이며 로봇의 실제 시간이 아닙니다. 큐브도 물리 공간이 아니라 배치, 미래 행동 수, 행동 차원의 텐서입니다.
'''

### Slide 7. X-VLA: Motivation

지금까지 VLA의 사전지식에 대한 설명이었고 본론으로 넘어가겠습니다. X-VLA 논문에서는 로봇 환경이 달라지면 팔의 구조뿐 아니라 카메라 위치와 행동 데이터의 분포도 달라지는데, 이때 데이터를 단순히 섞는 것만으로는 성능이 좋아지지 않을 수 있습니다. 그래서 이종간 데이터를 어떻게 잘 학습데이터로 활용해서 다양한 로봇팔 환경에 적응 시킬 수 있는가에 대해 설명합니다.

### Slide 8. X-VLA: Shared Structure

전체 구조는 이렇습니다. 대부분의 정책 가중치를 공유하면서, 데이터셋 아이디에 맞는 프롬프트와 행동 입출력 변환을 선택하게 합니다.

사전학습에는 29만건의 에피소드와 일곱가지 구성의 로봇팔을 사용합니다. 같은 로봇도 카메라 구성이 다르면 다른 도메인으로 분류합니다.

### Slide 9. Soft Prompt

소프트 프롬프트는 사람이 작성한 문장이 아닌 학습 가능한 벡터입니다.

이 모델은 도메인마다 32개의 토큰과 1,024차원을 가지고 있습니다. 선택한 프롬프트를 영상·언어 토큰, 제어 토큰과 함께 입력합니다.

그러면 셀프 어텐션이 이 조건을 참조하면서 행동을 예측합니다. 정답 행동과의 오차를 줄이는 과정에서, 프롬프트도 도메인에 유용한 값으로 학습됩니다.

### Slide 10. X-VLA Forward Pipeline

상세한 파이프라인 입니다. Florence2 VLM으로 영상과 지시를 처리하고, 추가 시점은 Vision Encoder로 처리합니다. 두 시점은 공유됩니다.

오른쪽에서는 현재 상태, 노이즈 행동, 생성 진행도를 결합해 제어 토큰을 만듭니다. 여기에 소프트 프롬프트를 더해 Transformer를 통과해 출력에서 제어 토큰에 해당하는 특징을 선택하고, 이를 도메인 별 출력층으로 변환해 Action 갱신값과 Gripper 상태를 예측합니다.

''' (질문하면 대답하기)
이 그림은 한 번의 계산이며, Action 생성에는 앞에서 본 반복 갱신이 포함됩니다.
'''

### Slide 11. Heterogeneous Demonstration
  
이종간 데이터 처리과정입니다.

서로 다른 Action을 공통으로 변환하고 미래 4초의 궤적을 30개의 anchors로 요약합니다. 또한 도메인과 궤적을 섞어 특정 데이터에 치우치는 현상을 줄입니다.

### Slide 12. Pre-training & Adaptation

학습은 사전학습과 도메인 적응으로 나뉩니다. 먼저 여러 도메인의 프롬프트와 공유 모델을 함께 학습합니다.

그리고 공유 모델을 고정하고, 1,000번 동안 새 프롬프트와 Action Head 부분을 준비합니다. 이후에 공유 모델도 함께 미세조정합니다.

### Slide 13. Analyze

위 그래프는 모델과 데이터 규모를 늘렸을 때 검증 오차가 줄어드는 경향을 나타내는 그래프입니다.

아래는 학습된 소프트 프롬프트를 시각화한 결과입니다. 유사한 로봇 구성에서 토큰이 가까이 섞여 있는것을 볼 수 있습니다. 이는 프롬프트가 데이터 ID마다 별개의 표현을 외운 것에 더해, 두 설정이 가진 공통점도 반영한 것으로 볼 수 있습니다.

### Slide 14. Benchmark

벤치마크 결과입니다. X-VLA는 0.9B 규모로 모든 상황에서 우상향 한 것을 볼 수 있습니다.

### Slide 15. PEFT & Quantitative Analysis

위 표에서는 각 항목별 기여도에 대한 결과입니다. 소프트 프롬프트를 더한 단계에서 9.2% 좋아졌고 최종 95.8%로 데이터 처리, 모델 확장, 적응 절차까지 합친 결과입니다.

### Slide 16. Demonstration Folding Clothes

벤치마크 뿐만 아니라 실제 X-VLA를 양팔 로봇에 적용하여 옷을 펴고 접는 작업을 1,200회 반복 적응한 뒤 시간당 약 33회 접기가 가능하였습니다.

### Slide 17. X-VLA: Contributions

정리하자면, 이 논문은 이질적인 행동 데이터를 정리하고 도메인별 소프트 프롬프트로 공유 정책을 조건화한 뒤, 새 로봇에 추가 적응시키는 방법입니다.

마지막으로 논문의 저자는 Future Works로 처음 보는 로봇에 학습 없이 바로 연결하거나, 매우 긴 작업을 안정적으로 수행하는 문제는 남아 있다고 설명합니다.

### Slide 18. 마무리

이상으로 발표를 마치겠습니다. 감사합니다.


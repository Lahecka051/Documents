# CoPE-VideoLM 논문 리뷰

> **논문:** CoPE-VideoLM: Leveraging Codec Primitives For Efficient Video Language Modeling  
> **저자:** Sayan Deb Sarkar, Rémi Pautrat, Ondrej Miksik, Marc Pollefeys, Iro Armeni, Mahdi Rad, Mihai Dusmanu  
> **소속:** Microsoft Spatial AI Lab, Stanford University, ETH Zurich  
> **버전:** arXiv:2602.13191v2, 2026-03-30  
> **링크:** [논문](https://arxiv.org/abs/2602.13191) | [프로젝트 페이지](https://microsoft.github.io/CoPE/)  
> **리뷰 기준:** 논문 본문과 부록 전체 31쪽. 결과 수치는 저자가 보고한 값이며 별도 재현 실험을 수행한 것은 아니다.

---

## 1. 한 문장 요약

CoPE-VideoLM은 비디오의 모든 선택 프레임을 RGB 이미지로 복원한 뒤 동일한 수의 비전 토큰으로 바꾸는 대신, **I-frame만 기존 비전 인코더로 처리하고 P-frame은 코덱에 이미 저장된 motion vector와 residual을 8개의 작은 `Delta-token`으로 압축**하여, 시간적 변화 정보는 더 촘촘히 보면서 LLM의 입력 토큰 수와 time-to-first-token(TTFT)을 크게 줄이는 방법이다.

논문의 가장 중요한 아이디어는 다음 구분에 있다.

- 일반적인 프레임 선택 또는 사후 token pruning은 선택된 RGB 프레임을 먼저 디코딩하고 비전 인코더까지 통과시킨 뒤 토큰을 줄인다.
- CoPE-VideoLM은 대부분의 P-frame에서 **RGB 복원과 무거운 비전 인코더 경로 자체를 우회**한다.
- 따라서 이 방법의 장점은 LLM attention 토큰 감소뿐 아니라, 비전 front-end 계산을 줄인다는 데 있다.

저자가 보고한 최고 효율 수치는 다음과 같다.

- 시각 토큰 사용량 최대 **93% 감소**
- TTFT 최대 **86.2% 감소**: 2.39초에서 0.33초
- 64개 텍스트 토큰 생성 end-to-end latency 최대 **56.1% 감소**: 3.78초에서 1.66초
- 14개 비디오 이해 벤치마크에서 대체로 경쟁력 있는 정확도 유지
- 특히 LLaVA-Video-7B 대비 PerceptionTest, ActivityNet-QA, TempCompass, TOMATO, CVRR-ES, Video-TT, Video-MMMU, LVBench 등에서 향상

그러나 모든 벤치마크를 이긴 것은 아니다. NExT-QA, VideoMME, LongVideoBench에서는 기반 모델인 LLaVA-Video-7B보다 낮은 항목이 있으며, 8시간 비디오 결과는 실제 8시간 비디오 정확도나 latency 실험이 아니라 **토큰 예산에 대한 분석적 스케일링 계산**이다.

---

## 2. 연구 동기: 왜 기존 VideoLM의 프레임 표현이 비효율적인가?

### 2.1 긴 비디오는 단순히 context window만 늘려서 해결하기 어렵다

VideoLM은 보통 다음 순서로 비디오를 처리한다.

```text
비디오 -> 일부 RGB 프레임 선택 -> 비전 인코더 -> 프레임별 시각 토큰 -> LLM prefill -> 답변 생성
```

비디오가 길어지면 프레임 수도 증가한다. 프레임마다 비슷한 수의 시각 토큰을 넣는다면 시각 sequence 길이는 비디오 길이에 거의 비례한다. Transformer LLM에서 sequence 길이를 $L$이라고 하면 대략 다음 부담이 생긴다.

```math
\mathrm{KV\ memory} \propto L
```

```math
\mathrm{Prefill\ attention\ compute} \propto L^2
```

정확한 실행 시간은 FlashAttention, batching, 메모리 대역폭, 커널 구현 등에 따라 달라지지만, 입력 토큰이 길어질수록 prefill이 비싸진다는 방향은 변하지 않는다. 이 때문에 공개형 VideoLM은 흔히 비디오 길이와 무관하게 64개 같은 고정 개수의 프레임만 선택한다.

문제는 64개 프레임이 64초 비디오에는 1 FPS가 될 수 있지만, 1시간 비디오에서는 약 56초마다 한 프레임만 보는 수준이라는 점이다. 비디오가 길어져도 관찰량이 늘지 않으므로 다음 두 종류의 정보를 함께 놓칠 수 있다.

- **거시 사건:** 긴 구간 중간에 한 번만 발생하는 장면 전환, 등장인물 교체, 원인 사건
- **미시 변화:** 손동작, 접촉 직전의 움직임, 물체의 짧은 상태 변화, 행동 순서

### 2.2 반대로 모든 프레임을 RGB로 보는 것도 낭비다

인접한 비디오 프레임은 대부분의 배경과 물체 외형을 공유한다. 1 FPS로 줄여도 장면에 따라 상당한 중복이 남는다. 그런데 표준 VideoLM은 각 프레임을 독립 이미지처럼 취급하므로, 변화가 거의 없는 프레임에도 새로운 장면과 같은 $M$개의 토큰을 할당한다.

즉 기존 방식에는 상충하는 문제가 있다.

1. 프레임을 적게 선택하면 시간적 사건을 놓친다.
2. 프레임을 많이 선택하면 RGB 인코딩과 LLM prefill 비용이 급증한다.

### 2.3 논문의 관찰: 코덱은 이미 이 중복을 제거하고 있다

비디오 코덱은 오래전부터 인접 프레임의 중복을 다음처럼 표현한다.

- **I-frame:** 다른 프레임 없이 복원 가능한 완전한 기준 이미지
- **P-frame:** 이전 기준 영상에서 무엇이 이동했는지를 나타내는 motion vector와, 이동만으로 설명되지 않는 픽셀 차이를 나타내는 residual
- **B-frame:** 과거와 미래 프레임을 모두 참조하는 양방향 예측 프레임

따라서 코덱 bitstream에는 이미 다음 구조가 존재한다.

```text
드문 전체 장면 정보 + 자주 나타나는 작은 변화 정보
      I-frame       motion vector + residual
```

CoPE-VideoLM의 핵심 연구 질문은 다음과 같다.

> 코덱이 계산해 둔 시간적 희소성을 그대로 VideoLM의 토큰 할당 원리로 사용하면, 프레임 관찰 빈도는 유지하면서 RGB 처리와 LLM 토큰 비용을 함께 줄일 수 있는가?

### 2.4 선행 방법과의 차이

논문이 지적하는 선행 방법의 빈틈은 세 가지이다.

| 방법 계열 | 장점 | 남는 문제 |
|---|---|---|
| RGB keyframe sampling | 구현이 단순하고 기존 VLM 사용 가능 | 선택하지 않은 시간 구간을 잃고, 선택 프레임은 모두 비싸게 처리 |
| 사후 token pruning/merging | LLM에 들어가는 토큰 감소 | 모든 RGB 프레임의 디코딩과 비전 인코딩 비용은 이미 지불 |
| CoViAR, TEAM-Net 계열 | motion/residual을 compressed domain에서 활용 | 별도 CNN 중심이며 상호 의존성과 VideoLM 통합이 제한적 |
| Video-LaVIT | motion vector를 언어형 토큰으로 사용 | residual을 버려 appearance correction 정보 손실 |
| EMA | GOP 단위 요약으로 토큰 수 고정 | residual을 버리고 GOP 내부 시간 순서를 압축 과정에서 약화 |
| **CoPE-VideoLM** | motion과 residual을 모두 보존하고 I/P 토큰을 시간 순서대로 interleave | B-frame 미지원, 고정 fusion window, 코덱/bitrate 민감도 미검증 |

CoPE의 새로움은 motion vector 자체를 처음 사용했다는 데 있지 않다. 더 정확히는 다음 세 가지를 VideoLM 입력 안에서 동시에 구현한 데 있다.

1. motion과 residual을 모두 사용한다.
2. 각 변화 토큰을 I-frame 토큰 사이에 원래 시간 순서대로 배치한다.
3. GOP 전체를 고정 길이 한 덩어리로 요약하지 않고, 비디오 길이와 변화량에 따라 토큰 수가 늘어나는 variable-length 표현을 사용한다.

---

## 3. 전체 구조

논문의 실제 추론 경로를 단순화하면 다음과 같다.

```text
MPEG-4 비디오 bitstream
        |
        +-- I-frame RGB ----------------> frozen SigLIP ----------> M=196 RGB tokens
        |
        +-- P-frame motion vector ------> motion branch ----+
        |                                                  |
        +-- P-frame residual -----------> residual branch --+--> N=8 Delta-tokens
                                                                    |
시간 순서대로 [I tokens, Delta-tokens, ..., I tokens, ...] interleave
                                                                    |
텍스트 질문 토큰과 결합 -> Qwen2-7B 기반 LLM -> 다음 토큰 예측
```

여기서 중요한 점은 P-frame 추론 때 RGB target frame을 복원해 SigLIP에 넣지 않는다는 것이다. Delta-Encoder 사전학습 때만 reference/warped transformer를 사용해 RGB embedding 공간과의 관계를 학습하며, 최종 VideoLM 학습과 추론에서는 이 두 보조 transformer를 제거한다.

---

## 4. 기호와 tensor shape

| 기호 | 의미 | 논문 설정 또는 shape |
|---|---|---|
| $T$ | 비디오 프레임 수 | 가변 |
| $H,W,C$ | 높이, 너비, 채널 수 | 입력 해상도에 의존 |
| $d$ | 시각 token embedding 차원 | 부록에서 $d=1152$ |
| $M$ | I-frame 하나의 dense vision token 수 | $M=196$ |
| $K_{\tau}$ | motion query token 수 | 4 |
| $K_{\delta}$ | residual query token 수 | 4 |
| $N$ | P-frame 그룹 하나의 Delta-token 수 | $N=K_{\tau}+K_{\delta}=8$ |
| $s$ | 연속 P-frame fusion window | 기본 $s=30$ raw frames |
| $\tau^{(t)}$ | motion vector tensor | $\mathbb{Z}^{H\times W\times 2}$ |
| $\delta^{(t)}$ | residual tensor | $\mathbb{R}^{H\times W\times C}$ |
| $X_I^{(t)}$ | I-frame vision tokens | $\mathbb{R}^{M\times d}$ |
| $X_P^{(t)}$ | P-frame Delta-tokens | $\mathbb{R}^{N\times d}$ |

Motion vector는 원래 대개 최대 $16\times16$ macroblock마다 하나 존재한다. 저자는 downstream 모듈에 넣기 위해 이를 $H\times W\times2$의 희소 tensor로 만든다. Residual도 같은 공간 크기의 tensor로 만든다. 이 선택은 구현은 편하지만, 원래 bitstream의 block-wise sparse set 구조를 dense tensor 형태로 확장할 수 있다는 뜻이므로 완전한 zero-copy codec 처리는 아니다.

---

## 5. 코덱 이론과 식 (1): P-frame은 어떻게 현재 프레임을 나타내는가?

논문의 출발점은 P-frame 복원식이다.

```math
\widehat{I}^{(t)}_i
=
\widehat{I}^{(t-1)}_{i-\tau^{(t)}_i}
+
\delta^{(t)}_i
```

여기서 $i$는 현재 프레임의 픽셀 위치이다.

- $\tau^{(t)}_i$는 현재 위치 $i$가 이전 복원 프레임의 어느 위치에서 이동해 왔는지를 나타낸다.
- $\widehat{I}^{(t-1)}_{i-\tau^{(t)}_i}$는 이전 프레임을 motion-compensation한 값이다.
- $\delta^{(t)}_i$는 motion compensation만으로 설명되지 않는 색, 밝기, 경계, 가림, 양자화 차이를 보정한다.

이를 두 단계로 읽으면 쉽다.

```math
I_{\mathrm{motion}}^{(t)}(i)
=
\widehat{I}^{(t-1)}_{i-\tau^{(t)}_i}
```

```math
\widehat{I}^{(t)}(i)
=
I_{\mathrm{motion}}^{(t)}(i)
+
\delta^{(t)}_i
```

Motion vector만 사용하면 물체가 어디로 이동했는지는 알 수 있지만, 새로 드러난 영역, 조명 변화, 변형, 세밀한 외형 변화는 표현하기 어렵다. Residual은 바로 이 부족분을 채운다. 따라서 CoPE가 Video-LaVIT이나 EMA와 달리 residual을 버리지 않는 것은 appearance 정보를 보존하기 위한 핵심 설계이다.

또 하나 중요한 점은 $\widehat{I}^{(t-1)}$가 항상 원본 RGB 프레임은 아니라는 것이다. 이전 시점도 P-frame이었다면, 그보다 더 앞선 프레임에서 재귀적으로 복원된 영상이 기준이 된다. 코덱은 인코더와 디코더 양쪽이 같은 복원 reference를 쓰도록 설계해 예측 오차의 누적 불일치를 막는다.

### 왜 B-frame은 제외했는가?

B-frame은 과거뿐 아니라 미래 I/P-frame도 참조한다. 압축률은 더 좋지만 다음 문제가 생긴다.

- display order와 decode order가 다를 수 있다.
- 미래 reference가 준비될 때까지 기다려야 한다.
- causal streaming VideoLM의 시간 순서와 바로 맞지 않는다.

저자는 실시간성과 causal 처리에 더 자연스러운 I/P 구조만 사용한다. 다만 실제 H.264/HEVC 동영상에는 B-frame이 흔하므로, 이 선택은 적용 범위를 제한한다.

---

## 6. 식 (2)-(4): RGB 프레임과 변화 프레임에 서로 다른 토큰 예산을 배정한다

각 프레임은 다음처럼 구분된다.

```math
F^{(t)}=
\begin{cases}
I^{(t)}, & F^{(t)}\text{가 I-frame인 경우} \\
P^{(t)}=\left(\tau^{(t)},\delta^{(t)}\right), & F^{(t)}\text{가 P-frame인 경우}
\end{cases}
```

I-frame은 기존 frozen vision encoder $\phi_{\mathrm{RGB}}$로 처리한다.

```math
X_I^{(t)}
=
\phi_{\mathrm{RGB}}\left(I^{(t)}\right)
\in
\mathbb{R}^{M\times d}
```

P-frame은 Delta-Encoder $\phi_{\Delta}$로 처리한다.

```math
X_P^{(t)}
=
\phi_{\Delta}\left(\tau^{(t)},\delta^{(t)}\right)
\in
\mathbb{R}^{N\times d},
\qquad N\ll M
```

기본 설정은 $M=196$, $N=8$이다. 즉 P-frame 그룹 하나는 I-frame 하나보다 시각 토큰이 $196/8=24.5$배 적다.

이 식의 의미는 단순한 압축률보다 **토큰의 역할 분담**에 있다.

- $X_I^{(t)}$는 장면의 절대적인 appearance anchor이다.
- $X_P^{(t)}$는 그 anchor 이후 무엇이 움직이고 달라졌는지를 나타내는 update이다.
- LLM은 두 종류를 동일한 embedding 차원 $d$에서 시간 순서대로 받는다.

따라서 CoPE의 표현은 매 시점에 전체 상태를 반복하는 방식이 아니라, 드문 full state와 잦은 state update를 결합한 event-driven 표현에 가깝다.

---

## 7. 토큰 절감은 어디서 나오는가?

### 7.1 일반식

한 처리 구간에 I-frame이 $K$개, fused P-frame 그룹이 $P$개 있다고 하자. CoPE의 시각 토큰 수는 다음과 같다.

```math
L_{\mathrm{CoPE}}
=
KM+PN
```

같은 $K+P$개의 시간 지점을 모두 RGB로 처리하면 다음과 같다.

```math
L_{\mathrm{RGB}}
=
(K+P)M
```

따라서 토큰 절감률은 다음과 같다.

```math
R_{\mathrm{save}}
=
1-
\frac{KM+PN}{(K+P)M}
=
\frac{P(M-N)}{(K+P)M}
```

이 식은 CoPE의 trade-off를 직접 보여준다.

- $N/M$이 작을수록 P-frame 하나를 RGB로 처리하지 않을 때의 이득이 크다.
- I-frame 비율 $K/(K+P)$이 작을수록 전체 토큰 절감은 커진다.
- 반대로 I-frame을 너무 줄이면 장면의 절대 appearance anchor가 부족할 수 있다.
- $P$를 늘리거나 fusion window $s$를 줄이면 시간 해상도는 높아지지만 토큰 수가 증가한다.

### 7.2 논문의 8초 GOP 예시

실험에서는 비디오를 30 FPS, GOP 크기 240으로 재인코딩한다. 따라서 GOP 하나는 8초이다.

모든 raw P-frame을 처리한다면 다음과 같다.

```math
L_{\mathrm{native\ CoPE}}
=
M+239N
```

모든 240개 프레임을 RGB로 처리하면 다음과 같다.

```math
L_{\mathrm{native\ RGB}}
=
240M
```

$M=196$, $N=8$을 대입하면 각각 2,108개와 47,040개이다. 이론적 시각 토큰 절감률은 약 95.5%이다. 다만 논문의 주 실험은 모든 30 FPS 지점을 처리하지 않고 30개 P-frame을 묶어 1 FPS 상당으로 만든다.

8초 GOP 안의 1 FPS 시점 8개를 모두 RGB로 처리하면 다음과 같다.

```math
L_{\mathrm{1FPS\ RGB}}
=
8M
=
8\times196
=
1568
```

CoPE의 `1I+7P` 표현은 다음과 같다.

```math
L_{\mathrm{1FPS\ CoPE}}
=
M+7N
=
196+7\times8
=
252
```

따라서 동일한 8개 시간 지점을 표현한다는 단순 비교에서 토큰 절감률은 다음과 같다.

```math
1-\frac{252}{1568}
\approx
0.839
```

즉 약 83.9%이다. 논문 제목과 초록의 최대 93% 절감은 이 계산과 모순되지 않는다. 93%는 데이터셋별 길이가 다른 실제 평가에서 `64 RGB frames` 기준과 가장 compact한 sampling 설정을 비교한 headline 수치이다. 비교 기준이 다르므로 두 숫자를 섞어 해석하면 안 된다.

### 7.3 계산량 관점

RGB vision encoder 한 번의 비용을 $C_{\mathrm{vision}}$, Delta-Encoder 한 번의 비용을 $C_{\Delta}$라고 쓰면 front-end 비용은 개념적으로 다음과 같다.

```math
C_{\mathrm{RGB}}
=
(K+P)C_{\mathrm{vision}}
```

```math
C_{\mathrm{CoPE}}
=
KC_{\mathrm{vision}}
+
PC_{\Delta}
```

$C_{\Delta}\ll C_{\mathrm{vision}}$라면 P-frame이 많을수록 이득이 커진다. LLM 쪽에서는 입력 길이가 $L_{\mathrm{text}}+L_{\mathrm{vis}}$일 때 prefill self-attention의 단순화된 계산량이 다음처럼 증가한다.

```math
C_{\mathrm{attn}}
\propto
\left(L_{\mathrm{text}}+L_{\mathrm{vis}}\right)^2
```

그러므로 시각 토큰 감소는 KV-cache 메모리를 대략 선형으로 줄이는 데 그치지 않고, 긴 입력의 attention 계산을 더 크게 줄일 수 있다. 다만 논문은 $C_{\Delta}$의 FLOP, resolution별 latency, codec primitive 추출 시간을 분리 보고하지 않았다. TTFT 86.2% 감소를 단순히 토큰 비율의 제곱으로 계산해서는 안 된다.

---

## 8. P-frame fusion은 무엇을 하는가?

원래 P-frame은 바로 이전 복원 프레임에 의존하므로, 중간 P-frame을 단순히 건너뛰면 motion vector와 residual의 reference가 깨진다. CoPE는 $s$개의 연속 P-frame 변화를 결합해 $F^{(t-s)}$를 기준으로 한 변화로 만든다고 설명한다. 기본값 $s=30$은 30 FPS 입력에서 1초당 하나의 fused update를 만든다.

Warp 연산을 $W_{\tau}$라고 쓰면 한 단계는 다음과 같다.

```math
\widehat{I}^{(t)}
=
W_{\tau^{(t)}}\left(\widehat{I}^{(t-1)}\right)
+
\delta^{(t)}
```

두 단계를 전개하면 다음 구조가 된다.

```math
\widehat{I}^{(t)}
=
W_{\tau^{(t)}}
\left(
W_{\tau^{(t-1)}}\left(\widehat{I}^{(t-2)}\right)
+
\delta^{(t-1)}
\right)
+
\delta^{(t)}
```

즉 여러 프레임을 합칠 때 motion vector는 좌표계를 따라 합성해야 하고, 이전 residual도 이후 warp를 거쳐야 한다. 단순히 motion vector와 residual을 원소별로 더하는 문제는 아니다.

논문은 fused primitive가 $F^{(t-s)}$에 대한 결합 변화라고 설명하지만, 이 합성 연산을 식이나 pseudocode로 완전히 정의하지 않는다. 따라서 fusion은 성능과 재현성에 매우 중요한 부분임에도 논문만으로 정확한 구현을 복원하기 어렵다.

---

## 9. Delta-Encoder: motion과 residual을 8개 토큰으로 만드는 과정

Delta-Encoder는 1,500만 개 미만의 파라미터를 갖는다고 보고되며, motion과 residual에 서로 다른 inductive bias를 주는 두 branch로 구성된다.

### 9.1 Motion branch

입력 motion tensor는 다음과 같다.

```math
\tau^{(t)}
\in
\mathbb{Z}^{H\times W\times2}
```

부록에 따른 처리 순서는 다음과 같다.

1. motion vector 값을 min-max normalization하여 $[-1,1]$로 변환한다.
2. motion field를 겹치지 않는 $16\times16$ patch로 나눈다.
3. 각 patch를 $16^2\times2=512$차원 벡터로 펼친다.
4. 공유되는 2-layer MLP를 각 patch에 적용해 $d=1152$차원 feature로 만든다.
5. 공간 grid의 전체 feature에 $K_{\tau}=4$개의 learned query가 attention한다.
6. query 위치의 출력만 motion token으로 남긴다.

공간 grid 크기는 다음과 같다.

```math
H_G=\frac{H}{16},
\qquad
W_G=\frac{W}{16}
```

MLP 뒤의 feature shape은 다음과 같다.

```math
T_{\tau}^{(t)}
\in
\mathbb{R}^{(H_GW_G)\times d}
```

논문의 식 (5)는 다음과 같다.

```math
\tau_{\mathrm{tok}}^{(t)}
=
\theta_{\mathrm{motion}}
\left(
\mathrm{MLP}\left(\tau^{(t)}\right)
\right)
\in
\mathbb{R}^{K_{\tau}\times d}
```

Transformer는 4개 layer, 9개 attention head, hidden dimension $d=1152$, PreNorm residual block을 사용한다고 설명된다. Learned query는 전체 motion grid를 읽고 소수의 latent token으로 요약하는 역할을 한다.

### 9.2 Residual branch

Residual 입력은 다음과 같다.

```math
\delta^{(t)}
\in
\mathbb{R}^{H\times W\times C}
```

Residual은 local texture와 appearance correction을 포함하므로 MLP 대신 마지막 global pooling 이전까지의 truncated ResNet-18로 처리한다. 출력은 motion branch와 같은 $H_G\times W_G$ grid resolution을 갖는다. 별도의 residual transformer와 $K_{\delta}=4$개 query가 이를 압축한다.

논문의 식 (6)은 다음과 같다.

```math
\delta_{\mathrm{tok}}^{(t)}
=
\theta_{\mathrm{residual}}
\left(
\mathrm{ResNet}\left(\delta^{(t)}\right)
\right)
\in
\mathbb{R}^{K_{\delta}\times d}
```

### 9.3 최종 P-frame token

두 branch 출력을 sequence 방향으로 연결한다.

```math
X_P^{(t)}
=
\left[
\tau_{\mathrm{tok}}^{(t)};
\delta_{\mathrm{tok}}^{(t)}
\right]
\in
\mathbb{R}^{N\times d}
```

```math
N
=
K_{\tau}+K_{\delta}
=
4+4
=
8
```

이 구조는 motion과 appearance correction을 별도 전문가로 처리한 뒤 같은 차원의 token으로 결합한다. 다만 query 수는 구간의 실제 entropy와 무관하게 항상 8개이므로, 엄밀한 의미의 entropy-adaptive token allocation은 아니다. 정적인 구간과 급격한 변화 구간이 동일한 token 수를 받는다.

### 9.4 시간 순서 결합

최종 시각 sequence는 다음처럼 자연 시간 순서대로 연결된다.

```math
X_{\mathrm{vis}}
=
\mathrm{Concat}_{t}
\left(
\begin{cases}
X_I^{(t)}, & I\text{-frame} \\
X_P^{(t)}, & P\text{-frame group}
\end{cases}
\right)
```

이 sequence를 텍스트 질문과 함께 기존 LLM에 넣는다. 논문은 별도의 timestamp embedding을 명시하지 않는다. 고정 1 FPS에서는 positional order가 시간을 암묵적으로 나타내지만, fusion window나 sampling 간격이 가변적일 때 실제 시간 간격을 얼마나 정확히 전달하는지는 불분명하다.

---

## 10. 2단계 학습과 식 (7)-(10)

### 10.1 왜 Delta-Encoder를 바로 LLM과 학습하지 않는가?

Codec primitive는 SigLIP이 본 적 없는 입력 modality이다. 이를 처음부터 LLM과 함께 학습하면 모델은 한꺼번에 다음을 해결해야 한다.

1. block motion의 의미를 학습한다.
2. residual의 appearance 의미를 학습한다.
3. 두 신호를 결합한다.
4. RGB vision token과 호환되는 표현을 만든다.
5. I/P token을 시간적으로 융합한다.
6. 언어 질문에 답하도록 학습한다.

저자는 이를 분리한다.

- **Stage 1:** Delta-Encoder가 codec 변화량으로 target RGB feature를 설명하도록 사전학습
- **Stage 2:** 사전학습된 Delta-Encoder를 VideoLM에 넣고 instruction tuning

### 10.2 식 (7): 이전 reference의 image token

```math
X_I^{(t-1)}
=
\phi_{\mathrm{RGB}}
\left(I^{(t-1)}\right)
```

Frozen SigLIP이 이전 reference frame을 $M$개의 image token으로 바꾼다.

### 10.3 식 (8): motion-conditioned feature warp

```math
\widehat{X}_{\mathrm{warped}}^{(t-1)}
=
\theta_{\mathrm{ref}}
\left(
X_I^{(t-1)},
\tau_{\mathrm{tok}}^{(t)}
\right)
```

Reference transformer는 이전 image token과 motion token을 함께 보고, motion compensation 뒤의 feature를 예측한다. 이는 픽셀 복원식의 $W_{\tau^{(t)}}(I^{(t-1)})$에 대응하는 latent-space 연산이다.

### 10.4 식 (9): residual-conditioned correction

```math
\widehat{X}_P^{(t)}
=
\theta_{\mathrm{warped}}
\left(
\widehat{X}_{\mathrm{warped}}^{(t-1)},
\delta_{\mathrm{tok}}^{(t)}
\right)
```

Warped transformer는 motion으로 옮긴 feature에 residual 정보를 반영해 target frame의 image token을 예측한다. 코덱 식의 `motion compensation + residual correction`을 feature 공간에 옮긴 구조이다.

식 (10)의 patch-wise regression target은 실제 target frame을 frozen vision encoder에 넣은 결과이다.

```math
X_I^{(t)}
=
\phi_{\mathrm{RGB}}
\left(\widehat{I}^{(t)}\right)
\in
\mathbb{R}^{M\times d}
```

Loss는 다음과 같다.

```math
\mathcal{L}_{\mathrm{MSE}}
=
\frac{1}{M}
\sum_{i=1}^{M}
\left\lVert
X_I^{(t)}(i)
-
\widehat{X}_P^{(t)}(i)
\right\rVert_2^2
```

Global contrastive loss와 달리 동일한 patch index끼리 맞추므로 공간적으로 일관된 target feature를 복원하도록 유도한다.

여기에는 중요한 해석상의 차이가 있다. 8개의 Delta-token을 196개의 RGB token과 직접 일대일 MSE로 비교하는 것이 아니다.

```text
(motion, residual)
      -> 8 Delta-tokens
      -> reference/warped auxiliary transformers
      -> M개의 predicted target features
      -> frozen SigLIP의 M개 target features와 MSE
```

따라서 이 사전학습은 **8개 Delta-token이 보조 decoder를 통해 RGB feature를 복원할 만큼 충분한 정보를 갖게 하는 것**이지, 각각의 Delta-token 자체를 특정 RGB patch와 직접 동일하게 만드는 것은 아니다.

### 10.5 Stage 2와 실제 추론

사전학습 뒤에는 reference transformer와 warped transformer를 제거한다. VideoLM fine-tuning과 실제 추론에서는 다음만 남는다.

- I-frame용 frozen vision encoder
- motion/residual용 Delta-Encoder
- 기존 multimodal projector와 Qwen2-7B LLM

따라서 식 (7)-(10)은 추론 알고리즘이 아니라 **학습용 scaffold**이다. 추론 때 P-frame마다 RGB feature 196개를 재구성하지 않고, 8개의 Delta-token을 그대로 LLM에 전달한다.

Downstream 목적함수는 표준 next-token prediction이다. 이를 명시적으로 쓰면 다음과 같다. 이 식은 논문의 번호가 붙은 식이 아니라 표준 목적함수를 풀어 쓴 것이다.

```math
\mathcal{L}_{\mathrm{LM}}
=
-\sum_j
\log p
\left(
y_j
\mid
y_{1:j-1},X_{\mathrm{vis}}
\right)
```

Stage 1은 변화 token의 표현 공간을 만들고, Stage 2는 LLM이 full-state token과 update token의 관계를 실제 질의 응답에 사용하도록 만든다.

---

## 11. 학습 및 평가 설정

### 11.1 모델과 데이터

| 항목 | 설정 |
|---|---|
| Base VideoLM | LLaVA-Video-7B |
| Vision encoder | SigLIP |
| Language model | Qwen2-7B |
| Codec | MPEG-4로 재인코딩 |
| Frame rate | 30 FPS |
| GOP 크기 | 240 raw frames, 즉 8초 |
| 기본 fusion | $s=30$, effective 1 FPS |
| Instruction data | LLaVA-Video-178K와 3개 QA subset, 총 1.39M samples |
| Main fine-tuning sampling | GOP당 4 keyframes + 4 P-frame groups |
| Main learning rate | $10^{-5}$ |
| Effective batch | 128 |
| 총 보고 학습량 | 약 21K A100 GPU-hours |

Delta-Encoder 사전학습은 다음 조건이다.

- 16개 A100 GPU에서 2일
- 113K iterations
- global batch size 1024
- 논문 표현상 per-GPU learning rate $6.25\times10^{-5}$
- AdamW, cosine scheduler, warmup 1,000 steps

VideoLM fine-tuning은 64개 A100 GPU, 10.9K steps, global batch 128로 수행된다. $10{,}900\times128\approx1.395$M이므로 1.39M instruction sample을 대략 1 epoch 소비한 설정과 일치한다.

### 11.2 평가 범위

논문은 14개 benchmark를 네 종류로 묶는다.

1. **General Video QA:** PerceptionTest, NExT-QA, ActivityNet-QA, VideoMME
2. **Temporal/Motion:** TempCompass, TOMATO, CVRR-ES, MVBench
3. **Long-form/Instruction:** LongVideoBench, LVBench, Video-TT, Video-MMMU
4. **Spatial/3D:** ScanQA, SQA3D

평가는 `lmms-eval`을 사용한다. ActivityNet-QA는 Table 1에서 `gpt-4o-2024-11-20`, Table 2에서 prior work와 맞추기 위해 Azure `gpt-3.5-turbo-0613`을 judge로 사용한다. 따라서 두 표의 ActivityNet 숫자는 서로 같은 평가 함수의 결과가 아니며 직접 비교하면 안 된다.

### 11.3 긴 비디오 sampling

기존 공개 VideoLM은 흔히 비디오 길이와 관계없이 64개 RGB 프레임을 사용한다. CoPE는 I-frame과 P-frame group을 합쳐 1 FPS로 최대 64개 GOP까지 처리한다. 64 GOP는 GOP당 8초이므로 512초이다.

512초보다 긴 비디오는 모든 GOP를 처리하는 것이 아니라 64개 GOP를 균등 샘플링한 뒤, 선택된 GOP 안의 I/P-frame만 인코딩한다. 따라서 CoPE가 512초를 넘는 모든 시간 지점을 계속 1 FPS로 완전 관찰한다고 이해하면 잘못이다.

---

## 12. 핵심 성과 1: 같은 keyframe 수에서 Delta-token이 주는 이득

Table 1은 동일한 수의 RGB keyframe을 쓰는 LLaVA-Video와, 그 사이에 작은 P-frame token을 추가한 CoPE를 비교한다. Token 비율은 LLaVA-Video의 기본 64-frame 입력을 100으로 둔 데이터셋별 평균이다.

| RGB keyframe 밀도 | 방식 | PerceptionTest token / acc | NExT-QA token / acc | ActNet-QA token / acc |
|---|---|---:|---:|---:|
| 1/GOP | LLaVA | 5.3 / 60.4 | 8.3 / 77.9 | 21.6 / 61.7 |
| 1/GOP | CoPE + 7P | 6.9 / **65.5** | 11.2 / **78.3** | 29.7 / **62.3** |
| 2/GOP | LLaVA | 9.7 / 62.1 | 15.9 / 79.2 | 42.5 / 62.9 |
| 2/GOP | CoPE + 6P | 11.3 / **68.7** | 18.6 / **80.4** | 50.4 / **63.6** |
| 4/GOP | LLaVA | 18.6 / 63.6 | 31.0 / 80.5 | 84.3 / 63.6 |
| 4/GOP | CoPE + 4P | 20.1 / **70.3** | 33.5 / **82.1** | 91.7 / **64.8** |
| 고정 64 frames | LLaVA | 100 / 67.9 | 100 / 83.2 | 100 / 64.1 |

이 표에서 가장 강한 증거는 **matched keyframe count 비교**이다.

- `1I`에 7개 작은 P update를 더하면 PerceptionTest가 60.4에서 65.5로 5.1 상승한다.
- `2I`에 6P를 더하면 62.1에서 68.7로 6.6 상승한다.
- `4I`에 4P를 더하면 63.6에서 70.3으로 6.7 상승한다.

NExT-QA와 ActivityNet-QA의 증가 폭은 더 작지만 모든 matched row에서 양수이다. 즉 단순히 keyframe 수를 바꾼 것이 아니라, codec update가 추가 정보를 제공한다는 주장을 지지한다.

다만 효율 이득은 데이터셋과 비디오 길이에 따라 다르다. PerceptionTest의 `1I+7P`는 baseline token의 6.9%만 사용하지만, ActivityNet의 `4I+4P`는 91.7%를 사용한다. 따라서 “항상 93% 절감”이 아니라 **최대 93% 절감**이라고 읽어야 한다.

---

## 13. 핵심 성과 2: 기반 모델 LLaVA-Video-7B와의 전체 benchmark 비교

동일한 7B 기반 모델과의 차이를 표로 정리하면 다음과 같다. ActivityNet 값은 Table 2의 동일 judge 조건이다.

| Benchmark | LLaVA-Video-7B | CoPE-7B | 차이 |
|---|---:|---:|---:|
| PerceptionTest | 67.9 | **70.3** | +2.4 |
| NExT-QA | **83.2** | 82.1 | -1.1 |
| ActivityNet-QA | 56.5 | **60.3** | +3.8 |
| VideoMME, subtitle 없음 | **63.3** | 61.9 | -1.4 |
| VideoMME, subtitle 있음 | **69.7** | 69.4 | -0.3 |
| TempCompass | 66.6 | **68.9** | +2.3 |
| TOMATO | 24.9 | **28.3** | +3.4 |
| CVRR-ES | 43.6 | **49.4** | +5.8 |
| MVBench | 58.6 | **61.9** | +3.3 |
| Video-TT | 41.8 | **45.5** | +3.7 |
| Video-MMMU | 36.1 | **38.2** | +2.1 |
| LVBench | 44.2 | **46.4** | +2.2 |
| LongVideoBench | **58.2** | 56.9 | -1.3 |

### 결과 해석

- 변화와 순서가 중요한 TempCompass, TOMATO, CVRR-ES에서 일관된 향상이 나타난다. Codec primitive가 temporal inductive bias로 작동한다는 논문의 중심 가설과 잘 맞는다.
- General QA에서는 혼합 결과이다. PerceptionTest와 ActivityNet은 개선되지만 NExT-QA와 VideoMME는 낮다.
- Long-form에서도 Video-TT, Video-MMMU, LVBench는 개선되지만 LongVideoBench는 낮다.
- 논문의 표에 포함된 proprietary model은 대부분 훨씬 높은 절대 성능을 보인다. 따라서 적절한 결론은 “비슷한 규모의 공개 모델 중 여러 항목에서 강한 결과”이지, 모든 VideoLM을 대상으로 한 절대 SOTA는 아니다.

논문 본문은 MVBench에서 약 3.0 향상이라고 서술하지만 표의 숫자 $61.9-58.6$은 3.3이다. 이는 작은 서술/반올림 불일치이다.

### 데이터 규모에 대한 저자의 설명

부록 Table E.1에서 저자는 VideoMME 열세를 데이터 구성으로 설명한다.

| LLaVA-Video 학습 단계 | 데이터 수 | NExT-QA | PerceptionTest | VideoMME |
|---|---:|---:|---:|---:|
| LLaVA-Hound | 0.25M | 64.4 | 51.4 | 54.1 |
| + LLaVA-Video-178K | 1.58M | 80.1 | 57.1 | 63.2 |
| + 3 QA datasets | 1.64M | 80.1 | 69.0 | 61.9 |
| + LLaVA-OV images | 2.74M | 83.2 | 67.9 | 63.4 |
| LLaVA-Video sampled | 1.08M | 73.2 | 55.9 | 59.6 |
| CoPE, video + 3 QA | 1.39M | **82.1** | **70.3** | 61.9 |

Image alignment data를 더하면 VideoMME가 61.9에서 63.4로 오르고, QA data 추가 시에는 63.2에서 61.9로 내려간다는 관찰은 CoPE의 VideoMME 결과가 data mixture와 관련될 가능성을 지지한다.

하지만 이것은 CoPE 구조에서 data mixture 하나만 바꾼 통제 실험이 아니다. 다른 LLaVA checkpoint를 가져온 간접 비교이고 표의 데이터 수도 정확히 같지 않다. 따라서 “codec formulation의 한계가 아님을 확인했다”기보다 “data mixture 설명과 일관된다”가 더 정확하다.

---

## 14. 핵심 성과 3: 실제 latency와 분석적 long-context scaling

64개의 텍스트 token을 생성하는 latency를 1 FPS 입력과 단일 consumer-grade GPU에서 측정한 결과이다.

| 입력 설정 | TTFT | baseline 대비 감소 | E2EL | baseline 대비 감소 |
|---|---:|---:|---:|---:|
| LLaVA 64 RGB frames | 2.39 s | - | 3.78 s | - |
| CoPE 1I+7P, 총 8I+56P | **0.33 s** | **86.2%** | **1.66 s** | **56.1%** |
| CoPE 2I+6P, 총 16I+48P | 0.51 s | 78.7% | 1.71 s | 54.8% |
| CoPE 4I+4P, 총 32I+32P | 0.90 s | 62.3% | 2.28 s | 39.7% |

TTFT 감소 폭이 E2EL보다 큰 이유는 자연스럽다.

- TTFT는 긴 visual prefix를 처리하는 vision encoding과 LLM prefill의 영향을 크게 받는다.
- 답변 token을 순차 생성하는 decode 구간은 시각 입력 길이 감소의 직접 효과가 상대적으로 작다.
- 따라서 입력 처리 시간을 크게 줄여도 전체 생성 시간의 감소율은 더 낮다.

### 8시간 비디오 결과는 무엇인가?

논문은 가장 compact한 설정이 1M-token context에서 1 FPS 기준 약 8시간 비디오까지 표현할 수 있다고 보인다. 그러나 Figure 4(b)는 프로젝트 페이지에서도 theoretical plot이라고 설명된다. 이는 다음을 뜻한다.

- 8시간 비디오에 대해 end-to-end QA 정확도를 측정한 결과가 아니다.
- 8시간 입력의 실제 peak memory, TTFT, E2EL을 실행해 보고한 결과가 아니다.
- token generation rule을 긴 길이로 외삽한 budget 계산이다.

따라서 강하게 뒷받침된 주장은 “보고된 짧은 입력 조건에서 TTFT가 크게 줄었다”와 “토큰 산술상 더 긴 비디오를 context에 넣을 수 있다”이다. “8시간 비디오를 높은 정확도로 실시간 이해했다”는 결론은 논문이 증명하지 않았다.

### Runtime 재현 정보의 부족

논문은 GPU를 `single consumer-grade GPU`라고만 적는다. 다음 정보가 없다.

- 정확한 GPU 모델과 VRAM
- 입력 video 길이와 해상도
- precision과 batch size
- warm-up과 반복 횟수
- latency의 평균, median, p95, p99 및 분산
- codec parsing, MPEG-4 재인코딩, primitive 추출 시간이 TTFT에 포함되는지
- `Runtime and Memory` 절의 실제 peak GPU memory 수치

따라서 상대 개선은 유의미한 신호이지만, 다른 장비에서 절대 latency를 재현하기에는 정보가 부족하다.

---

## 15. 공간 및 3D 질의 응답 성과

부록은 SQA3D와 ScanQA를 약 15초 비디오로 변환해 평가한다. 이 설정에서는 GOP 120, fusion $s=10$, 6 keyframes + 6 P-frame groups를 사용한다.

| 설정 | token 비율 | SQA3D EM | EM-R | ScanQA CIDEr | BLEU-4 | METEOR | ROUGE | EM |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| LLaVA-Video zero-shot | 100% | 48.5 | - | 88.7 | 3.1 | 17.7 | 44.6 | - |
| CoPE zero-shot | 25.78% | 46.5 | 49.8 | 70.9 | 7.1 | 14.7 | 38.2 | - |
| CoPE fine-tuned | 25.78% | **56.6** | **59.3** | **96.9** | **14.9** | **19.1** | **46.4** | **27.5** |

Zero-shot에서는 1/4 수준의 token으로 SQA3D EM이 2.0 낮아 비교적 가깝지만, ScanQA의 CIDEr, METEOR, ROUGE는 더 낮다. BLEU-4는 오히려 높다. 그러므로 “25% token으로 완전히 동일한 공간 성능”보다는 “압축 표현에도 상당한 공간 정보가 남아 있다”가 타당하다.

Fine-tuning 뒤에는 크게 개선되지만, 동일한 ScanQA/SQA3D fine-tuning을 한 LLaVA-Video baseline이 표에 없다. 저자도 다른 VideoLM이 같은 fine-tuning으로 개선될 수 있음을 인정한다. 따라서 fine-tuned 수치를 codec 방식의 독립적 우월성으로 해석하면 안 된다.

---

## 16. 사후 token pruning과의 비교

부록 Table G.1은 다음 결과를 보고한다.

| 방법 | ActivityNet-QA | NExT-QA | VideoMME, subtitle 없음 |
|---|---:|---:|---:|
| FastV | 47.95 | 81.1 | 57.5 |
| DyCoke | 47.88 | 81.1 | 57.4 |
| PLLaVA | 47.59 | 81.0 | 56.9 |
| VisionZip | 45.42 | 78.5 | 54.2 |
| LLaVA-Scissor | 47.89 | 81.2 | 57.4 |
| **CoPE** | **60.28** | **82.1** | **61.9** |

CoPE가 모든 열에서 높다. 방법론적으로도 CoPE는 P-frame의 vision encoder 호출 자체를 피하므로, dense RGB token을 만든 뒤 줄이는 방법보다 TTFT를 줄일 여지가 크다.

하지만 완전한 matched-budget 실험은 아니다.

- pruning baseline은 기본 token budget의 50%를 사용한다.
- CoPE는 설정에 따라 그보다 훨씬 작은 budget을 사용한다.
- post-hoc inference pruning과 codec-aware retraining은 학습 조건도 다르다.
- 표 caption은 TTFT가 더 빠르다고 서술하지만 Table G.1 자체에는 방법별 TTFT 수치가 없다.

따라서 이 표는 CoPE의 실용적 결과가 강하다는 증거이지만, codec-native 표현과 최적의 pruning 방법 중 어느 요소가 얼마만큼 기여하는지를 완전히 분리한 실험은 아니다.

---

## 17. Ablation: 실제로 무엇이 성능에 기여하는가?

부록 H.1-H.3의 ablation은 main run보다 작은 설정으로 수행되었다.

- PerceptionTest, NExT-QA, ActivityNet-QA를 합친 60K samples
- 16개 A100에서 2일
- 따라서 아래 절대 점수는 본문의 Table 1-3과 직접 비교할 수 없다.

### 17.1 Delta-token 수

| P-frame group당 token 수 | PerceptionTest | NExT-QA |
|---:|---:|---:|
| 2 | 63.26 | 75.04 |
| 4 | 65.68 | 76.08 |
| 8 | **67.33** | **77.37** |
| 16 | 67.69 | 77.89 |

2개에서 8개까지는 성능이 뚜렷하게 증가한다. 그러나 8개에서 16개로 늘렸을 때 증가는 각각 0.36, 0.52에 그친다. 저자는 이 포화 경향을 근거로 8개를 선택한다.

이 결과는 8개가 현재 데이터와 모델에서 좋은 효율 지점임을 보여주지만, 모든 codec, bitrate, 해상도, task에서 8개가 충분하다는 이론적 보장은 아니다. 또한 2, 4, 16개 설정에서 motion query와 residual query를 각각 몇 개로 나눴는지 명시되지 않는다.

### 17.2 2단계 사전학습의 효과

| Delta-Encoder 사전학습 | VideoLM fine-tuning | PerceptionTest | NExT-QA |
|---|---|---:|---:|
| 없음 | 있음 | 63.45 | 74.56 |
| 있음 | 있음 | **67.33** | **77.37** |

사전학습은 각각 3.88, 2.81의 개선을 제공한다. Codec 신호를 RGB latent space에 먼저 맞추는 것이 optimization을 쉽게 한다는 설명을 지지한다.

그러나 2단계 방식은 별도의 113K-step 사전학습 계산을 더 사용한다. 따라서 이 표는 `patch-wise MSE라는 목적함수`의 순수 효과와 `추가 optimization/data exposure` 효과를 분리하지 못한다. 같은 총 계산량의 one-stage control이 필요하다.

### 17.3 LLM이 Delta-token을 정말 사용하는가?

| 추론 조건 | PerceptionTest | NExT-QA |
|---|---:|---:|
| Delta-token을 0으로 치환 | 64.41 | 74.21 |
| 정상 Delta-token | **67.33** | **77.37** |

정상 token 사용 시 각각 2.92, 3.16 높다. LLM이 I-frame만 보고 Delta-token을 무시하는 것이 아니라는 증거이다.

다만 zero vector는 학습 분포 밖의 입력일 수 있다. 다음 통제가 추가되면 시간 정보 이용을 더 강하게 입증할 수 있다.

- 다른 비디오의 Delta-token으로 교체
- Delta-token의 시간 순서를 shuffle 또는 reverse
- motion-only와 residual-only
- token 제거와 attention mask
- 평균 token 또는 matched-noise token으로 치환

### 17.4 Codec-aware training 효과와 추론 token 효과

| Sampling | PT | NExT-QA | ActNet | TempCompass | TOMATO |
|---|---:|---:|---:|---:|---:|
| 1 keyframe | 63.1 | 78.0 | 61.8 | 60.3 | 22.4 |
| + 7 P groups | **65.5** | **78.3** | **62.3** | **62.4** | **26.2** |
| 2 keyframes | 66.2 | 79.4 | 62.5 | 64.5 | 26.5 |
| + 6 P groups | **68.7** | **80.4** | **63.6** | **65.6** | **27.1** |
| 4 keyframes | 68.1 | 80.8 | 63.7 | 66.2 | 26.9 |
| + 4 P groups | **70.3** | **82.1** | **64.8** | **68.9** | **28.4** |

Codec-aware training을 받은 CoPE 모델에서 inference Delta-token을 제거해 I-frame만 넣어도 원래 LLaVA keyframe baseline보다 대체로 강하다. 여기에 실제 P-frame token을 추가하면 모든 표 항목이 더 오른다.

따라서 저자의 해석은 두 효과가 더해진다는 것이다.

1. Codec primitive와 함께 학습한 representation learning 효과
2. 추론 때 실제 변화 신호를 읽는 효과

단, 본문 Table 1과 비교하면 2-keyframe ActivityNet에서 codec-trained I-only 62.5는 LLaVA 62.9보다 낮다. 그러므로 codec-aware training만으로 모든 matched setting을 이긴다고 표현하면 과장이다.

### 17.5 Fusion window와 FPS

| Effective FPS | Fusion $s$ | 표의 P groups | 보고 token 수 | TempCompass | MVBench |
|---:|---:|---:|---:|---:|---:|
| 1 | 30 | 4 | 880 | 66.89 | 59.98 |
| 2 | 15 | 8 | 1,760 | **68.86** | **61.87** |
| 3 | 10 | 12 | 2,640 | 68.73 | 61.67 |

1 FPS에서 2 FPS로 올리면 각각 1.97, 1.89 향상한다. 3 FPS에서는 2 FPS보다 0.13, 0.20 낮지만 1 FPS보다는 여전히 높다. 모델이 $s=30$만으로 학습되었기 때문에, 저자는 3 FPS의 작은 하락을 train-test mismatch 가능성으로 설명한다.

이 결과는 fusion window가 단순한 압축 숫자가 아니라 모델이 보는 temporal granularity를 바꾸는 학습 조건임을 보여준다. 실제 시스템에서는 여러 $s$를 섞어 학습하거나 motion/residual entropy에 따라 $s$를 적응적으로 선택하는 편이 자연스럽다.

그러나 Table H.5에는 산술 불일치가 있다. Caption은 I-frame 하나가 $196+14$, P group 하나가 $8+2$ token이며 다음 식을 쓴다고 설명한다.

```math
L_{\mathrm{table}}
=
(196+14)K
+
(8+2)P
```

동시에 모든 설정이 `1 keyframe per GOP`라고 쓰여 있다. 그러면 $K=1$, $P=4$일 때 $210+40=250$이어야 하지만 표는 880을 보고한다. 880은 $K=4$, $P=4$를 대입할 때 나온다. 1,760과 2,640도 각각 $K=P=8,12$일 때 나온다. 따라서 caption의 `1 keyframe/GOP`, P group 수, `tokens/GOP` 정의 중 하나 이상이 잘못 표기되었거나 설명이 생략되었다.

### 17.6 Next-frame retrieval

| 표현 | Recall@1 | Recall@2 | Recall@5 |
|---|---:|---:|---:|
| SigLIP, 이전 RGB frame | 11.12 | 47.11 | 78.65 |
| Delta-Encoder 경로 | **30.09** | **77.15** | **94.86** |

이 결과는 codec primitive가 다음 상태를 식별하는 데 유용한 변화 정보를 압축해 보존한다는 증거이다. 그러나 이를 미래 예측 성능으로 부르면 안 된다.

- SigLIP baseline은 $I^{(t-1)}$만 본다.
- CoPE 경로는 $I^{(t-1)}$뿐 아니라 실제 target transition에서 추출한 $\tau^{(t)}$와 $\delta^{(t)}$도 받는다.
- 또한 배포 모델이 아니라 reference/warped auxiliary transformer가 남아 있는 pre-training checkpoint를 사용한다.

따라서 정확한 해석은 **target transition primitive의 conditional representation fidelity**를 확인한 실험이다. 과거만 보고 미래를 맞히는 next-frame prediction은 아니다.

---

## 18. 논문이 강하게 입증한 것과 아직 입증하지 못한 것

| 주장 | 근거 수준 | 판단 |
|---|---|---|
| 작은 codec token이 sparse keyframe을 보완한다 | 강함 | Matched keyframe 비교와 zero-out ablation에서 일관된 향상 |
| Temporal task에 유용한 inductive bias다 | 비교적 강함 | TempCompass, TOMATO, CVRR-ES 및 FPS ablation이 지지 |
| 비전 인코더 이전에 중복을 줄여 TTFT를 낮춘다 | 강함 | 64개 temporal position latency에서 62.3-86.2% TTFT 감소 |
| 8개 Delta-token이 좋은 효율 지점이다 | 현재 설정에서는 강함 | 2/4/8/16 token ablation에서 8 이후 포화 |
| 2단계 학습이 도움이 된다 | 강함, 원인 분리는 불완전 | 사전학습 유무 차이는 크지만 추가 compute가 통제되지 않음 |
| 모든 14개 benchmark에서 더 좋다 | 입증되지 않음 | NExT-QA, VideoMME, LongVideoBench 등에서 기반 모델보다 낮음 |
| Codec 종류와 bitrate가 달라도 일반화한다 | 입증되지 않음 | MPEG-4 한 설정 중심, sensitivity 실험 없음 |
| 8시간 비디오를 실제로 정확하게 처리한다 | 입증되지 않음 | 토큰 capacity 외삽이며 실제 8시간 QA/latency 실험 없음 |
| Delta-Encoder 전체가 15M 미만이다 | 논문 설명만으로 검증 곤란 | 명시된 transformer 폭/층수와 parameter 산술이 맞지 않음 |
| 온디바이스에서 end-to-end로 빠르다 | 아직 입증되지 않음 | consumer GPU 수치만 있고 codec 추출, 전력, edge NPU 측정 없음 |

---

## 19. 강점

### 19.1 문제를 올바른 계층에서 푼다

기존 압축 연구 상당수는 이미 만든 RGB token을 LLM 앞에서 줄인다. CoPE는 비디오 codec이라는 더 앞단에서 중복 구조를 가져와 비전 인코더 호출과 LLM token을 함께 줄인다. 시스템 관점에서 중요한 차이이다.

### 19.2 Motion과 appearance를 함께 보존한다

Motion vector만 사용하면 위치 변화는 보지만 appearance correction을 잃는다. Residual branch를 병행함으로써 새로 드러나는 영역, 변형, 경계, 색 변화 정보를 보완한다. 이는 식 (1)에서 직접 정당화된다.

### 19.3 시간 순서를 유지한다

GOP 전체를 단일 summary로 만드는 대신 I-token과 P-token을 원래 순서대로 interleave한다. Action order와 causal reasoning에 더 자연스럽다.

### 19.4 기존 LLM 구조를 바꾸지 않는다

최종 token 차원을 기존 vision token과 맞추기 때문에, Qwen2-7B의 attention architecture나 next-token loss를 바꾸지 않는다. 다른 VideoLM에 이식할 가능성이 있다.

### 19.5 실험 범위와 통제 실험이 비교적 넓다

일반 QA, temporal, long-form, spatial을 모두 다루고, matched keyframe, token 수, pre-training, zero-out, inference token, FPS, retrieval ablation을 제공한다. 특히 headline score보다 `같은 I-frame 수 + P-token` 비교가 설득력 있다.

---

## 20. 한계와 재현성 문제

### 20.1 저자가 직접 인정한 한계

1. **B-frame 미지원:** 미래 reference가 필요한 비인과적 의존성을 처리하지 않는다.
2. **Tensorized primitive:** raw block vector와 quantized DCT coefficient를 직접 쓰지 않는다.
3. **고정 P-frame fusion window:** 영상 내 motion 정도가 달라도 동일한 $s$를 사용한다.
4. **Codec sensitivity 미검증:** codec 종류, bitrate, encoding quality 영향이 open question이다.

### 20.2 Codec 신호는 semantic motion 자체가 아니다

Motion vector는 optical flow의 정확한 대체물이 아니다. Codec encoder가 rate-distortion 최적화를 위해 선택한 block displacement이므로 다음에 민감할 수 있다.

- encoder implementation과 preset
- bitrate와 quantization
- camera motion
- texture가 적은 영역
- block 크기와 search range
- scene cut와 compression artifact

따라서 codec primitive가 좋은 inductive bias인 것은 맞지만, 의미론적으로 보장된 움직임 표현은 아니다.

### 20.3 MPEG-4 재인코딩 비용과 배포 조건

실험은 모든 영상을 MPEG-4, 30 FPS, GOP 240으로 재인코딩한다. 실제 입력이 H.264, HEVC, AV1 또는 B-frame 중심이면 transcoding이나 별도의 추출 경로가 필요할 수 있다. 이 전처리 비용이 TTFT/E2EL에 포함되었는지 명확하지 않다.

또한 hardware decoder API가 motion vector와 pixel-domain residual을 항상 application tensor로 직접 노출하는 것은 아니다. Residual을 얻기 위해 entropy decoding, inverse quantization, inverse transform이 필요할 수 있으므로 codec primitive가 계산적으로 완전히 무료인 것은 아니다.

### 20.4 Delta-Encoder 파라미터 수 설명의 불일치

논문은 전체 Delta-Encoder가 15M 미만이라고 한다. 그러나 부록은 motion/residual branch 각각에 hidden dimension $d=1152$, 4-layer, 9-head transformer가 있다고 설명한다.

표준 dense multi-head attention의 Q/K/V/O projection만 계산해도 layer당 대략 다음이다.

```math
4d^2
=
4\times1152^2
\approx
5.31\ \mathrm{M}
```

FFN을 제외해도 4 layers와 2 branches이면 약 42.5M이다. 여기에 truncated ResNet-18과 motion MLP가 더해진다. 따라서 논문의 구조 설명과 `<15M` 주장을 동시에 만족하려면 다음 중 하나가 추가로 필요하다.

- 더 작은 internal attention width
- low-rank projection
- layer 또는 branch weight sharing
- 표준 transformer와 다른 FFN/attention 구조
- 더 작은 residual backbone
- 파라미터 수 또는 구조 표기의 오류

공개 코드나 layer별 parameter table 없이는 어느 경우인지 확인할 수 없다. 프로젝트 페이지는 이 리뷰 작성 시점인 2026-09-01에도 code와 demo를 `Coming Soon`으로 표시한다.

### 20.5 Residual branch의 grid 설명

부록은 global pooling 전까지의 ResNet-18 convolution을 사용하면서 motion branch와 같은 $H/16\times W/16$ grid를 얻는다고 쓴다. 표준 ResNet-18의 마지막 feature map은 보통 output stride 32이다. 마지막 stage의 stride를 바꾸었는지, 일부 stage를 제거했는지 설명이 필요하다.

### 20.6 GOP와 `여러 keyframes per GOP`의 정의

고정 GOP 240이라면 native codec I-frame은 첫 프레임 하나이고 나머지는 P-frame이라고 부록이 설명한다. 그런데 evaluation에서는 2 또는 4 `keyframes per GOP`를 사용한다. 이것이 다음 중 무엇인지 명확하지 않다.

- P-frame 일부를 RGB로 완전 복원해 모델 keyframe처럼 처리
- 설정마다 codec GOP를 다시 구성
- codec GOP와 sampling GOP라는 두 개념을 혼용

이 정의는 token 산정과 실제 RGB decode 비용에 영향을 준다.

### 20.7 누락된 codec 구현 정보

- FFmpeg 또는 추출 library와 정확한 command
- MPEG-4 profile, bitrate, quantization, pixel format
- 입력 해상도와 scene-cut 설정
- motion vector 단위와 scale
- min-max normalization이 frame별인지 dataset-global인지
- motion이 상수일 때 min=max 처리
- residual이 inverse-transform된 pixel residual인지 transform coefficient인지
- $s$개 P-frame의 motion/residual 합성 알고리즘
- sparse tensor 저장 형식과 실제 메모리 이동량

### 20.8 학습 및 평가 재현 정보

- random seed, 반복 횟수, 표준편차, confidence interval 없음
- image/video input resolution 불명확
- optimizer의 weight decay와 beta, precision, gradient clipping/accumulation 불명확
- system prompt, conversation template, decoding temperature/top-p 불명확
- vision encoder, projector, LLM 중 Stage 2에서 정확히 무엇을 freeze하는지 parameter-level 목록 없음
- pre-training frame-pair sampling과 checkpoint selection 방법 불명확

0.3-1.0 정도의 개선은 seed variance보다 작은지 판단하기 어렵다. 특히 $N=8$과 $N=16$의 작은 차이는 통계적 유의성을 알 수 없다.

### 20.9 비교의 공정성

- Target benchmark의 train split 일부가 1.39M instruction corpus에 포함되므로 PerceptionTest, NExT-QA, ActivityNet 결과는 완전한 zero-shot이 아니다.
- 외부 모델은 모델 크기, 데이터, resolution, sampling, subtitle 조건이 다르다.
- Pruning 비교는 동일 token budget과 동일 training 조건이 아니다.
- Spatial fine-tuning 결과는 CoPE만 target dataset으로 fine-tune한 뒤 다른 zero-shot VideoLM과 함께 제시한다.
- ActivityNet judge가 표마다 다르다.

### 20.10 필요한 추가 ablation

- motion-only 대 residual-only
- 동일 temporal coverage에서 RGB frame을 8 token으로 압축한 control
- 동일 총 compute의 one-stage 대 two-stage
- MPEG-4/H.264/HEVC/AV1과 bitrate/quality sweep
- rapid cut, camera shake, low light, compression artifact 구간
- OCR, small object, counting, grounding breakdown
- adaptive fusion과 mixed-$s$ training
- 여러 seed와 confidence interval
- Delta-Encoder, codec extraction, LLM prefill의 component latency

---

## 21. 정성 결과는 어떻게 보아야 하는가?

부록 Figure 6-8은 TV show, 육상, 장작 패기, 하키, 요리 영상에서 여러 차례 질문을 수행하며 75.5-85.7% 정도의 token 감소와 큰 TTFT speedup을 보인다. 시간적 사건, 인물 관계, 부상 원인, 동작, 작은 물체에 대해 그럴듯하고 세부적인 답변을 제시한다.

하지만 다음 이유로 정성 그림은 보조 증거이다.

- 성공 사례만 있고 failure case가 없다.
- 비교 대상 LLaVA-Video의 실제 답변이 제시되지 않는다.
- ground truth와 정답 판정 근거가 없다.
- 두 번째 질의부터 두 모델 모두 KV-cache를 재사용한다.
- Caption과 panel의 speedup 표기가 일부 맞지 않는다. 예를 들어 Figure 6 caption은 8.2배, panel 숫자 6.90/0.87은 약 7.9배이다.

정성 그림은 “가능한 동작 사례”를 보여주지만 평균 품질이나 failure distribution을 증명하지는 않는다.

---

## 22. 온디바이스 및 실시간 시스템 관점

### 기대되는 장점

- 카메라 또는 저장 비디오는 이미 codec domain에 있으므로, 적절한 API가 있다면 redundant RGB materialization을 줄일 수 있다.
- 짧은 visual prefix는 LLM prefill, KV-cache, DRAM traffic을 함께 줄인다.
- P-frame이 과거에만 의존하므로 causal live stream과 잘 맞는다.
- Multi-turn 대화에서는 더 짧은 cached visual sequence를 재사용할 수 있다.

### 실제 장벽

- Qwen2-7B 자체가 일반 mobile memory에 크므로 LLM quantization이 별도로 필요하다.
- Delta-Encoder가 8개 token만 내더라도, ResNet-18과 wide transformer의 edge NPU latency는 별도 문제이다.
- 일반 hardware decoder가 필요한 primitive를 zero-copy로 노출하지 않을 수 있다.
- B-frame 입력은 그대로 지원하지 않는다.
- Sparse primitive를 $H\times W$ tensor로 펼치면 원래 codec sparsity의 memory-bandwidth 장점을 일부 잃을 수 있다.
- Random seek는 이전 I-frame부터 codec state를 복원해야 한다.
- Packet loss나 corrupted reference가 이후 P-frame 표현에 전파될 수 있다.

실제 배포 가능성을 확인하려면 최소한 다음 latency를 분리 측정해야 한다.

```text
demux
-> entropy decode / primitive extraction
-> tensorization 및 host-device transfer
-> I-frame vision encoder
-> Delta-Encoder motion branch
-> Delta-Encoder residual branch
-> multimodal projector
-> LLM prefill
-> text decode
```

동일한 비디오와 질문에서 RGB baseline과 temporal coverage를 맞춘 뒤 CPU/GPU/NPU latency, peak memory, DRAM bandwidth, energy, TTFT p50/p95/p99를 비교해야 온디바이스 효율을 주장할 수 있다.

---

## 23. 종합 평가

| 평가 항목 | 판단 |
|---|---|
| 문제 설정 | 매우 좋음. VideoLM이 codec의 중복 제거를 버리고 다시 RGB 중복을 만드는 문제를 정확히 지적 |
| 핵심 아이디어 | 강함. `full state + compact update`를 시각 tokenization에 적용 |
| 수식적 일관성 | Codec 복원식과 2단계 학습 구조는 직관적이나 fusion 연산이 미정의 |
| 실험 범위 | 넓음. 14개 benchmark와 여러 ablation 제공 |
| 성능 주장 | 대체로 설득력 있으나 모든 benchmark 우위는 아님 |
| 속도 주장 | 보고 조건에서는 강함. 단 하드웨어와 측정 경계가 부족 |
| 장시간 주장 | 토큰 capacity 측면에서는 타당하나 실제 8시간 실험은 아님 |
| 재현성 | 현재 낮음-중간. code 미공개, codec/fusion/runtime 세부 누락, 일부 표 산술 불일치 |
| 실제 배포성 | 잠재력 큼. 하지만 primitive 접근성, B-frame, transcoding, edge front-end 측정이 필요 |

### 최종 판단

이 논문의 가장 큰 성과는 모든 benchmark에서 최고 점수를 얻은 것이 아니다. 더 중요한 성과는 **동일한 sparse I-frame에 극소수의 codec-derived update token을 추가했을 때 정확도가 일관되게 오르고, 동일한 64개 temporal position을 비교한 latency에서 TTFT를 62.3-86.2% 줄였다는 점**이다.

특히 PerceptionTest, TempCompass, TOMATO, CVRR-ES처럼 시간 변화가 중요한 과제에서 향상된 결과는 motion vector와 residual이 VideoLM에 유용한 inductive bias가 된다는 주장을 뒷받침한다. CoPE는 단순한 token pruning보다 앞단에서 문제를 풀며, `I-frame = 상태`, `motion = 이동`, `residual = 보정`이라는 코덱 구조를 `RGB token = anchor`, `Delta-token = update`로 옮긴다.

반면 최대 93% token 감소, 8시간 비디오, codec 전반에 대한 일반성은 제한적으로 읽어야 한다. 93%는 64-frame baseline과 가장 compact한 sampling을 비교한 최대값이고, 8시간은 실제 QA 실험이 아니라 token capacity 외삽이다. B-frame 미지원, MPEG-4 한 설정, 고정 fusion, primitive extraction 비용 미보고, `<15M` 파라미터 설명과 구조의 불일치, Table H.5 산술 오류는 실제 재현과 배포 전에 반드시 해결되어야 한다.

결론적으로 CoPE-VideoLM은 **VideoLM 입력을 이미지의 연속이 아니라 코덱이 제공하는 상태와 변화의 연속으로 다시 정의했다는 점에서 이론적 동기와 시스템적 가치가 모두 분명한 연구**이다. 현재 논문은 유망한 방향과 강한 초기 실증을 제시하지만, 공개 코드와 codec-generalization 실험, component-level runtime, 더 엄격한 matched control이 있어야 범용적 효율 기법으로 확정할 수 있다.

---

## 24. 참고 링크

- [arXiv abstract](https://arxiv.org/abs/2602.13191)
- [PDF](https://arxiv.org/pdf/2602.13191)
- [공식 프로젝트 페이지](https://microsoft.github.io/CoPE/)

> 프로젝트 페이지 확인일: 2026-09-01. 해당 시점에 Code와 Demo는 `Coming Soon`으로 표시되어 있다.
